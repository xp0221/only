module.exports = {
  absolutePrefix : '/',
  relativePrefix: '',
  prodPublicPath: './'
}
/**
 * 当不使用路由时允许设置相对路径；
 * 设置 prodPublicPath = './' 时，
 * 必须设置:
 *  absolutePrefix = '/',
 *  relativePrefix = '',
 */